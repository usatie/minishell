#include <stdio.h>

int main(void)
{
	printf("The size of char is [%u]bite.\n", (unsigned)sizeof(char));
	printf("The size of  int is [%u]bite.\n", (unsigned)sizeof(int));
	printf("The size of long is [%u]bite.\n", (unsigned)sizeof(long));

	return 0;
}
